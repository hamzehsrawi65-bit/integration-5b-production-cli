# Integration 5B Production CLI

A production-ready command-line tool for comparing multiple classification models on a CSV dataset.

## Installation

Create and activate a virtual environment, then install dependencies:

```bash
python -m venv .venv
.venv\Scripts\activate
python -m pip install -r requirements.txt